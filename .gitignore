# Môi trường ảo
.venv/
__pycache__/
.pytest_cache/

# Dữ liệu nặng (Tránh đẩy lên Github làm chậm repo)
data/
*.csv

# Kết quả huấn luyện (ephemeral/scratch -- regenerated by re-running scripts)
outputs/
*.pth
*.png

# Curated milestone deliverables ARE committed (small, one checkpoint +
# plot + metrics per sprint milestone, not routine training junk) --
# override the *.pth/*.png/outputs/ rules above for this directory only.
!results/
!results/**

# Hệ thống
.DS_Store
.vscode/
